#include <iostream>
#include <string>

using ll = long long;

const int maxn = 2e5+7;
const ll inf = 1e17;

int main(int argc, char *argv[]) {  
	int T;
	std::cin >> T;
	while (T--) {
		ll n, a, b;
		std::string s;
		std::cin >> n >> a >> b;
		std::cin >> s;
		ll ans = 0;
	}
}