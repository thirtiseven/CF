#include <iostream>
#include <algorithm>
#include <string>

std::string s;

int main(int argc, char *argv[]) {  
	std::ios::sync_with_stdio(false);
	std::cin.tie(0); 
	while (std::cin >> s) {
		std::cout << "NO" << std::endl;
	}
	
}