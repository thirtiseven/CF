#include <iostream>
#include <algorithm>
#include <vector>

using ll = long long;
ll t, n, k;
std::vector<int> ans;

int main(int argc, char *argv[]) {  
	std::ios::sync_with_stdio(false);
	std::cin.tie(0); 
	std::cin >> t;
	while (t--) {
		ans.clear();
		std::cin >> n >> k;
		ll x = 1, nn = n;
		while (ans.size() < k) {
			
		}
	}
}